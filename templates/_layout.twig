<!doctype html>
<html lang="en-GB">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="robots" content="none">
    <title>{{ title is defined ? title ~ " | " }}'mon the Tigers!</title>
    <link href="/assets/css/app.css" rel="stylesheet">
    <style>
      body {
        position: relative;
      }

      body:before {
        content: "";
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url(/bg.jpg) no-repeat top left fixed;
        background-size: cover;
        opacity: 0.1;
      }
    </style>
  </head>
  <body class="min-h-screen">
    <div class="block mx-auto p-2 sm:p-8 max-w-screen-xl relative text-blue-800 text-lg pb-16">
      {% if craft.app.request.segments is not empty %}<a href="/">{% endif %}
        <h1 class="text-3xl text-center sm:pt-10 z-10">
          <span class="font-bold text-blue-600">C</span><span class="">oronavirus</span>
          <span class="font-bold text-blue-600">U</span><span class="">nitary</span>
          <span class="font-bold text-blue-600">N</span><span class="">ewton</span>
          <span class="font-bold text-blue-600">T</span><span class="">igers</span>
          <span class="font-bold text-blue-600">S</span><span class="">ingles</span>
        </h1>
        {% if craft.app.request.segments is not empty %}</a>{% endif %}

      <h2 class="text-center pb-10">Darts League</h2>
      <ul class="list-inline text-center">
        <li class="inline-block mb-5">
          <a class="bg-gray-100 rounded-sm border-2 px-2 py-1 {% if craft.app.request.segments is empty %}text-blue-600 border-blue-600{% else %}shadow-md border-blue-800 text-blue-800{% endif %}" href="/">Home</a>
        </li>
        <li class="inline-block mb-5">
          <a class="bg-gray-100 rounded-sm border-2 px-2 py-1 {% if 'games' in craft.app.request.segments %}text-blue-600 border-blue-600{% else %}shadow-md border-blue-800 text-blue-800{% endif %}" href="/games">Games</a>
        </li>
        <li class="inline-block mb-5">
          <a class="bg-gray-100 rounded-sm border-2 px-2 py-1 {% if 'players' in craft.app.request.segments %}text-blue-600 border-blue-600{% else %}shadow-md border-blue-800 text-blue-800{% endif %}" href="/players">Players</a>
        </li>
        <li class="inline-block mb-5">
          <a class="bg-gray-100 rounded-sm border-2 px-2 py-1 {% if 'order-of-merit' in craft.app.request.segments %}text-blue-600 border-blue-600{% else %}shadow-md border-blue-800 text-blue-800{% endif %}" href="/order-of-merit">Order of Merit</a>
        </li>
      </ul>
      <div class="pt-5">
        {% block content %}{% endblock %}
      </div>
    </div>
    <footer class="text-center text-sm text-gray-400 absolute bottom-0 left-0 right-0 min-h-5 pb-5">
      <p>Copyright {{ now|date('Y') }} Billy Fagan</p>
    </footer>
  </body>
</html>
